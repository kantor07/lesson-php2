<html>
    <head>
        <title>{{ title }}</title>
    </head>
    <body>
        <header>
            <div class='headerText'> 
                {{ title }}
            </div>
        </header>
        <main>
            <div class= "imgDiv"> 
                {% for photo in photos %}
                    <div class= "products">
                        <a href="/page2.php?bigImage= {{ photo.id }}">
                        <img class= "img" src="{{ photo.address_photo }}"></a>    
                        <h4 class="nameProduct"> {{ photo.photo_name }} </h4>
                    </div>
                {% endfor %}
             </div>
           
        </main>
    </body>
    <footer>

    </footer>
</html>

<style type="text/css">
 /*стилей не много, поэтому не подключала их отдельно*/
body {
    background-color: #d8da3d; 
}

.headerText {
    color:green;
    font-size: 30px;
    text-align: center;
    padding-bottom: 10px;
}

.imgDiv {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    flex-wrap: wrap;
    margin: 10px; 
}

.img {
    max-width: 300px;
    height: 200px;
    border: 2px solid green;
    margin: 10px;
} 

.img:hover {
    transform: scale(1.3); 
    border: 5px solid black; 
}

.products {
    text-align: center;
}

.nameProduct {
    text-align: center;
}
</style>